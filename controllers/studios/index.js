const create = require('./create');
const edit = require('./edit');
const remove = require('./remove');
const search = require('./search')
const projects = require('./proyects')

module.exports = {
    create,
    edit,
    remove,
    search,
    projects
}