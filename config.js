/**
 * Configuración de generación de datos originados por Faker. configuración
 * utilizada en ./DB/initDB.js
 */
 const fakerConfig = {
  };

  /**
 * Configuración de validaciones de peticiones mediante módulo Joi. Schemas
 * situados en ./schemas/index.js
 */
const joiConfig = {
  };
  
  /**Configuración de la edición y guardado de imágenes */
  const fotoConfig ={
  }
  
  module.exports = { fakerConfig, joiConfig, fotoConfig };
  