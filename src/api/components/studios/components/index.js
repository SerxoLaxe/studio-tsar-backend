const artists = require('./artists')
const clients = require('./clients')
const events = require('./events')
const projects = require('./projects')

module.exports = {artists, clients, events, projects}